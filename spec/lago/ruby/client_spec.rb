# frozen_string_literal: true

RSpec.describe Lago::Ruby::Client do
  it "has a version number" do
    expect(Lago::Ruby::Client::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
